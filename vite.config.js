import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
//  base: "/zunubakehouse/", // Update if your repo name differs
  plugins: [react()],
});
